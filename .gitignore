# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ------------------------------------------------------- 打包产物（重要）
# ★★ 本项目的产物目录是 dist0 / dist1 / … 和 build0 / build1 / …
#    ——**没有下划线**。只写 `dist/` `build/` 是挡不住的！
#    2026-09-15 发布前扫出来：光 dist* 就 11G，差点被提交进公开仓库。
#    改打包目录命名时，记得同步改这里。
dist/
dist[0-9]*/
dist_*/
build/
build[0-9]*/
build_*/
*.app/
.pyicache/
*.manifest

# ------------------------------------ 内部交接文档（只在本机用，不发布）
# START-HERE.md / HANDOFF.md 是「给下一个接手的人」的本机交接记录：
# 里面写着开发机的绝对路径、GitHub 账号、代理端口、密钥存放位置等，
# 属于本机环境信息，不适合放进公开仓库。留在这里，只是不让它们误入提交。
START-HERE.md
HANDOFF.md

# backup/ 里有 settings.json 的历史快照，**内含真实 API key**。
# 无论如何都不许入库 —— 一旦推到公开仓库，key 就永久留在 git 历史里了。
backup/

# ---------------------------------------------------------------- macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# --------------------------------------------------------------- 编辑器
.vscode/
.idea/
*.swp

# --------------------------------------------------- 运行期数据（绝不入库）
*.log
settings.json
# 离屏渲染出来的面板截图，属于临时产物
tools/_render/
